# in pycharm at the left of the next line there's a *, clicking on that you can go
# to the stub file
def sum(n, m):
    return n + m

# notice that the following line gives a type warning
# even though we don't have a type hint for the function
sum("ciao", 1)